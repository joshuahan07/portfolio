# ----- Client (safe to expose) -----
VITE_GITHUB_USERNAME=joshuahan07

# ----- API server (private) -----
# Used by /api/github-stats and can match VITE_* above when running locally.
GITHUB_USERNAME=joshuahan07

PORT=8787
ANTHROPIC_API_KEY=
